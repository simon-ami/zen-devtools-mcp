name: Publish disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain status
        run: echo "npm publishing is disabled for this personal Zen fork."
